import React, { Component } from 'react';
import Card from '../Card';

export default class Add extends Component {
  render() {
    return (
        <Card title="Add Category" collapse={1}>

        </Card>
    );
  }
}
